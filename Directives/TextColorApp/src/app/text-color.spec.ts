import { TextColor } from './text-color';

describe('TextColor', () => {
  it('should create an instance', () => {
    const directive = new TextColor();
    expect(directive).toBeTruthy();
  });
});
