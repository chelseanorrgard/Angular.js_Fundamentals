import { LettersOnlyPipe } from './letters-only-pipe';

describe('LettersOnlyPipe', () => {
  it('create an instance', () => {
    const pipe = new LettersOnlyPipe();
    expect(pipe).toBeTruthy();
  });
});
